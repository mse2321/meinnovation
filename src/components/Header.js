import React from 'react';

const Header = () => {
  const heading = 'Marcus Eubanks: Innovation';

  return (
    <header id="intro">
      <h1>{heading}</h1>
    </header>
  );
}

export default Header;

